---
# cloud_events configuration

events:
  supervisord:
    process_start:
      substring: "entered RUNNING state"
      regex: !!str (\w+) entered RUNNING
      sample: "INFO success: memcached_node entered RUNNING state, process has ..."
      message: "Process '$1' started on a node"
