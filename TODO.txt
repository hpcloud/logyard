* retrying drains more often come up as "running", due to delay in
  'connecting' stage (with tcp://google.com). do we need a "starting"
  state?

* fix 'starting' to account for errors raised during the starting stage.
